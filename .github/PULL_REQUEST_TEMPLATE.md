## Summary

<!-- What does this change do, and why? Link any issue it closes. -->

## Testing

<!-- How did you verify it? Commands run, and what you saw in the app. -->

## Checklist

- [ ] Signed off with `git commit -s` (DCO — see `CONTRIBUTING.md`)
- [ ] `cargo test --workspace` passes (run from `app/`); `cargo clippy` is clean-ish. This repo does **not** use `cargo fmt` — match the surrounding style.
- [ ] No private identifiers / secrets / absolute home paths in the diff
